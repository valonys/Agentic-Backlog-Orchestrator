# FastAPI and web server
fastapi>=0.100.0,<0.111.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.9

# Excel processing
openpyxl>=3.1.0
pandas>=2.0.0

# AI and LLM - CrewAI 1.x (upgraded from 0.11.2)
# CrewAI 1.x brings: async kickoff, hierarchical process, Flows, A2A support
# Breaking changes fixed:
#   - crew.kickoff() -> await crew.kickoff_async() in async contexts
#   - task.output_json now expects Pydantic model class (not JsonOutputParser)
#   - result is CrewOutput: use result.raw for string, result.json_dict for JSON
crewai==1.9.3
crewai-tools==1.9.3
langchain-openai==0.3.23
langchain==0.3.27
openai==1.83.0
# litellm: CrewAI 1.x uses it for LLM routing. Must stay <1.56 to keep openai pinned at 1.x.
litellm==1.55.12

# Environment and utilities
python-dotenv>=1.0.0
pydantic>=2.11.0,<3.0.0
pydantic-settings>=2.10.0

# Logging and monitoring
python-json-logger==2.0.7

# Database for caching
sqlalchemy==2.0.23

# Testing (optional)
pytest==8.0.1
pytest-asyncio==0.23.5
httpx>=0.27.0
